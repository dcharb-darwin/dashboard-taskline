# RTC Project Manager - Replit Environment Variables
# Copy these to Replit Secrets (Tools > Secrets in Replit UI)

# Database Configuration
# Use external MySQL database (PlanetScale, Railway, Supabase, etc.)
# Format: mysql://username:password@host:port/database
DATABASE_URL=mysql://user:password@host.example.com:3306/rtc_projects

# JWT Secret for session management
# Generate a random string: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Port (automatically set by Replit, no need to configure)
# PORT=3000
